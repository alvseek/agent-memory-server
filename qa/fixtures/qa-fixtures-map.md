# QA Fixtures Map — agent-memory-server

> Read-only audit of fixtures materials. Generated by /map-qa-instrument (last scan 2026-08-21).
> Links point to materials in-place; grades reflect discoverability. Fill gaps → /build-qa-test.

**Category status**: exists-tribal
**Owner**: /build-qa-test

## Found

| Material | Location | Grade | Notes |
|---|---|---|---|
| Fixture convention note | [README.md](README.md) | exists-documented | Header convention + the fidelity rule are documented; no fixture has been written against them |
| Whole-fleet source tree builder | [../../tests/data_migrations/test_importer.py](../../tests/data_migrations/test_importer.py) `_fake_source` | exists-tribal | Builds a complete markdown store — agents, episodes, knowledge index, shared layer. A genuine precondition-builder **inlined in a test suite**, the tribal home this category exists to surface |
| Seeded service/store builders | [../../tests/business_services/test_awaken.py](../../tests/business_services/test_awaken.py) `_service` · [../../tests/api_http/test_awaken_faces.py](../../tests/api_http/test_awaken_faces.py) `_seed` | exists-tribal | Same shape: reusable end-state builders that only one module can reach |
| Bare-schema DB builder | [../../tests/data_entities/test_schema.py](../../tests/data_entities/test_schema.py) `_db` | exists-tribal | Applies the DDL and seeds one agent so FK-bearing inserts are legal |

## Gaps

- **`qa/fixtures/` holds no fixture** — every real precondition-builder lives inside a pytest module, so nothing is composable and nothing is reachable by `/run-qa-test` Tactic B.
- **No fidelity headers**, because there are no fixtures — but note the builders above already satisfy the fidelity *rule* in spirit: `_fake_source` mirrors what the importer really reads.
- Correctly empty for now. Fixtures are built per-flow when a Tactic-B run needs one; this map records where the material would come from when that happens.

## Last scan

2026-08-21 · scanned qa/fixtures, tests/
