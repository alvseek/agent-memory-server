# QA Fixtures Map — agent-memory-server

> Read-only audit of fixtures materials. Generated by /map-qa-instrument (last scan 2026-08-21, rescanned after /build-qa-test).
> Links point to materials in-place; grades reflect discoverability. Fill gaps → /build-qa-test.

**Category status**: exists-documented
**Owner**: /build-qa-test

## Found

| Material | Location | Grade | Notes |
|---|---|---|---|
| Profile import-source fixture | [profile-source.sh](profile-source.sh) | exists-documented | Built 2026-08-21 by /build-qa-test. Four modes (`present`/`absent`/`markerless`/`blank-vision`) assembling a throwaway import source root; composes with `seed-meta.sh` via `MUNNIN_IMPORT_SOURCE`. Chain-run, fidelity-checked against the real authoring script, teardown-clean. The category's first real fixture |
| Fixture convention note | [README.md](README.md) | exists-documented | Header convention + the fidelity rule. As of today one fixture exists that follows them |
| Whole-fleet source tree builder | [../../tests/data_migrations/test_importer.py](../../tests/data_migrations/test_importer.py) `_fake_source` | exists-tribal | Builds a complete markdown store — agents, episodes, knowledge index, shared layer. A genuine precondition-builder **inlined in a test suite**, the tribal home this category exists to surface. Unchanged |
| Seeded service/store builders | [../../tests/business_services/test_awaken.py](../../tests/business_services/test_awaken.py) `_service` · [../../tests/api_http/test_awaken_faces.py](../../tests/api_http/test_awaken_faces.py) `_seed` | exists-tribal | Same shape: reusable end-state builders that only one module can reach. Unchanged |
| Bare-schema DB builder | [../../tests/data_entities/test_schema.py](../../tests/data_entities/test_schema.py) `_db` | exists-tribal | Applies the DDL and seeds one agent so FK-bearing inserts are legal. Unchanged |

## Gaps

- **One flow stage is covered, not a flow.** `profile-source.sh` builds the *author the profile file* stage. Every later stage of that flow is reached by a bench script, which is why no further fixture was built.
- **The four inlined builders above are still inlined.** Extracting them was out of scope for a per-flow build and remains the standing tribal gap this category was created to surface.
- **`markerless` is the one rung-3 mode** and has no upstream stage to check against — it models an accident rather than any stage's output. Recorded in the fixture header rather than left as an implied rung-1 claim.

## Last scan

2026-08-21 · scanned qa/fixtures, tests/
