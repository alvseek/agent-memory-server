# QA Seeds Map — agent-memory-server

> Read-only audit of seeds materials. Generated by /map-qa-instrument (last scan 2026-08-21).
> Links point to materials in-place; grades reflect discoverability. Fill gaps → /build-qa-bench.

**Category status**: exists-tribal
**Owner**: /build-qa-bench

## Found

| Material | Location | Grade | Notes |
|---|---|---|---|
| The markdown memory store (the real seed source) | `[AGENT-MEMORY-PATH]` — consumed by `munnin.data_migrations.importer --all` | exists-tribal | This is the project's actual INJECT source and it is **strategy B-shaped**: the store is authoritative and the DB is a rebuildable projection, so there is no dummy-fixture burden. Nothing under `qa/` says so |
| Point-in-time DB snapshots | [../../data/](../../data/) — five `valaskjalf-memory.db.bak-*` files | exists-tribal | ~76 MB, unreferenced by anything, kept as pre-change rollbacks. Logged as debt by the agent-entity plan (2026-08-20) |

## Gaps

- **`qa/seeds/` did not exist before this map** — created only to host this file. The seed *mechanism* is healthy; its documentation is absent.
- **Seed strategy is undeclared.** In practice it is *prod-snapshot-shaped* (re-import from the authoritative markdown store) rather than dummy fixtures, which is the honest choice here — purge-and-re-import costs seconds and cannot drift from the source. Record it so nobody builds dummy fixtures against a 27-agent store.
- **The backup sprawl needs a policy**: five unreferenced `.bak-*` files with no retention rule.

## Last scan

2026-08-21 · scanned qa/, data/, src/munnin/data_migrations
