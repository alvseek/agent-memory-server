# Copy to deploy/deploy.env (gitignored) and fill in. Used by deploy.sh / restart.sh (LOCAL machine).
DEPLOY_HOST=deploy@REPLACE_WITH_RACKNERD_HOST
DEPLOY_PATH=/home/deploy/agent-memory-server
SSH_KEY=~/.ssh/REPLACE_WITH_KEY
